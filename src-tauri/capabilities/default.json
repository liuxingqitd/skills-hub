{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Skills Hub window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
